<p style="color: green"><%= notice %></p>

<h1>Bills#index</h1>

<div id="bills">
  <% @bills.each do |bill| %>
    <%= render bill %>
    <p>
      <%= link_to "Show Bill", group_bill_path(bill.group_id, bill) %>
    </p>
  <% end %>
</div>

